version: '3.8'

services:
  backend:
    build: .
    container_name: linklet_backend_container
    command: python manage.py runserver 0.0.0.0:8000
    volumes:
      - .:/app
    ports:
      - "8000:8000"
    environment:
      # Direct Supabase Connection
      - DATABASE_URL=postgresql://postgres.qsiinpzrlyrdzaayukas:harshal_patil28.09@aws-1-ap-south-1.pooler.supabase.com:6543/postgres